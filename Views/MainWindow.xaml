<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero" x:Class="GothicSaveEditor.MainWindow"
        mc:Ignorable="d"
        Style="{DynamicResource WindowStyle}"
        Title="{Binding GSEVersion}" Height="400" MinHeight="400" Width="600" MinWidth="600">


    <Window.InputBindings>
        <KeyBinding Command="{Binding OpenCommand}" Gesture="Ctrl+A"/>
        <KeyBinding Command="{Binding SaveCommand}" Gesture="Ctrl+S"/>
        <KeyBinding Command="{Binding SaveAsCommand}" Gesture="Ctrl+Shift+S"/>
        <KeyBinding Command="{Binding MakeBackupCommand}" Gesture="Ctrl+B"/>
        <KeyBinding Command="{Binding CloseCommand}" Gesture="Ctrl+X"/>
        <KeyBinding Command="{Binding SearchCommand}" Key="Enter"/>
    </Window.InputBindings>


    <Grid Margin="5,0,5,0" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Menu Grid.Column="0" FontSize="14" Background="White">
                <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource Savegame}">
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource Open}" Command="{Binding OpenCommand}"/>
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource Save}" Command="{Binding SaveCommand}"/>
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource SaveAs}" Command="{Binding SaveAsCommand}"/>
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource MakeBackup}" Command="{Binding MakeBackupCommand}"/>
                    <Separator />
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource Close}" Command="{Binding CloseCommand}" />
                </MenuItem>
                <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource Tools}" Command="{Binding SettingsCommand}" Background="Transparent">
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource ApplyScript}" ItemsSource="{Binding Scripts, Mode=OneWay}" IsEnabled="{Binding Scripts.Count, Mode=OneWay}">
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="{x:Type MenuItem}">
                                <Setter Property="Header" Value="{Binding Name}" />
                                <Setter Property="Command" Value="{Binding ImportScriptCommand}" />
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                    <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource ExportVariables}" Command="{Binding ExportVariablesCommand}"/>
                </MenuItem>
                <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource Settings}" Command="{Binding SettingsCommand}"/>
                <MenuItem Style="{DynamicResource MenuItemStyle}" Header="{DynamicResource About}" Command="{Binding AboutCommand}"/>
            </Menu>
            <Menu Grid.Column="2" FontSize="14" Background="Transparent">
                <CheckBox IsThreeState="False" IsChecked="{Binding AutoSearch, Mode=TwoWay}" Content="{DynamicResource AutoSearch}"/>
            </Menu>
        </Grid>

        <Grid Grid.Row="1" Margin="0 5 0 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding SearchLine, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent"/>
                <Button Grid.Column="1" MinWidth="75" Content="{DynamicResource Search}" FontSize="14" Command="{Binding SearchCommand}" Background="Transparent"/>
            </Grid>
        </Grid>

        <DataGrid Margin="0, 2, 0, 0" Grid.Row="2" HeadersVisibility="Column" ItemsSource="{Binding DataGridVariables, Mode=OneWay}"  Background="Transparent" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False" CanUserSortColumns="False">
            <DataGrid.Resources>
                <Style TargetType="{x:Type ScrollBar}">
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Width="0.8*" Binding="{Binding FullName, Mode=OneWay}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{DynamicResource Variable}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Width ="0.2*" Binding="{Binding Value, Mode=TwoWay}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{DynamicResource Value}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StatusBar Grid.Row="3" Background="White">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <TextBlock FontSize="11" Text="{Binding LeftInfoLine, Mode=OneWay}" TextAlignment="Left" Background="Transparent"/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <TextBlock FontSize="11" Text="{Binding RightInfoLine, Mode=OneWay}" TextAlignment="Right" Background="Transparent"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
